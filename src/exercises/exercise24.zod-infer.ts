/** Exercício 24
 * Objetivo: Inferir tipos de DTOs a partir de schemas zod nas rotas.
 * Tarefas:
 * 1. Em server.ts, crie schemas `z.object` para inputs/outputs.
 * 2. Use `type RegisterBody = z.infer<typeof registerSchema>`.
 * 3. Substitua tipos manuais pelas inferências, mantendo segurança.
 * 4. Garanta que o VSCode autocomplete reflita mudanças no schema.
 */
export function placeholderExercise24() { /* implementação futura */ }
